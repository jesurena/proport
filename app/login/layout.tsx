import React from 'react';

export const metadata = {
    title: 'Login - TCD Portal',
    description: 'Login to access TCD Portal ticket management system.',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
